# check=skip=InvalidDefaultArgInFrom
ARG BASE_IMAGE
FROM ${BASE_IMAGE}

# No language runtime or package manager installed. Use this variant when the
# agent's work is language-agnostic (docs, shell, config, infra) or when the
# project brings its own toolchain.
