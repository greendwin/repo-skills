---
id: s04t11
slug: verify-and-test-python-3
status: pending
---

# Verify and test Python 3.10 support

`src/repo_skills/console.py` · `Console.running`:
> TODO: lets make sure that we support py3.10 and test it

Confirm the codebase actually runs on Python 3.10 (syntax/stdlib usage) and add coverage — e.g. a py3.10 tox/CI environment — so support is enforced rather than assumed. Remove the TODO once verified and tested.
