---
id: s08t08
slug: add-tests-for-source-list
status: pending
---

# Add tests for `source list` edge cases: (missing) and (not-inited)

`_source.py` (`source list`), tied to:
> TODO: test these branches on (missing) aand (not-inited)

No test coverage for `source list` when a registered source's repo directory doesn't exist ((missing) label) or when the source config isn't initialized ((not-inited) label). Add tests for both branches.
